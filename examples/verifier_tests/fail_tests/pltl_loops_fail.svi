// =====================================================
// pLTL in loops: expected failures
// Uncomment one at a time to test each failure.
// =====================================================

x : Int;
x := 0;

i : Int;
i := 0;

while i < 5 do
    x := x + 2;
    i := i + 1;
od

// After loop: x = 10
{ Once (x = 99) };                       // False (x was never 99)

// { Historically (x > 0) };             // False (x was 0 initially)
