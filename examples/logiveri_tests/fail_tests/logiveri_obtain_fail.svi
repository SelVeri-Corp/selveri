// =====================================================
// LogiVeri: obtain fail test — unsatisfiable constraint
// Should raise an error (IRRuntimeError / erroneous state)
// =====================================================

// No integer satisfies x > 10 AND x < 5 simultaneously
obtain(&x, {Exists x in Int . &x > 10 && &x < 5});
